apiVersion: v1
kind: Secret
metadata:
    name: {{ template "vydon-worker.fullname" . }}-service-evs-{{ include "vydon-worker.env-vars" . | sha256sum | trunc 8 }}
    namespace: {{ .Release.Namespace }}
    annotations:
        checksum/stringdata: {{ include "vydon-worker.env-vars" . | sha256sum }}
type: Opaque
stringData:
{{ include "vydon-worker.env-vars" . | indent 4 }}
