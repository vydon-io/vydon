apiVersion: v1
kind: Service
metadata:
  name: {{ template "vydon-api.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "vydon-api.labels" . | nindent 4 }}
    app: {{ template "vydon-api.fullname" . }}
spec:
  ports:
    - name: grpc # this is important!!! If it is anything other than "grpc", grpc routing will not work (at least when used with Istio) https://istiobyexample.dev/grpc/
      port: {{ .Values.servicePort }}
      protocol: TCP
      targetPort: {{ .Values.containerPort }}
  selector:
    app: {{ template "vydon-api.fullname" . }}
